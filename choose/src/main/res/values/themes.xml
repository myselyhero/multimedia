<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="multimedia_folder_window_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/multimedia_folder_window_enter</item>  <!--//进入时的动画-->
        <item name="android:windowExitAnimation">@anim/multimedia_folder_window_exit</item>    <!--//退出时的动画-->
    </style>

    <!-- 弹窗style -->
    <style name="dialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>